export type UserInfo = {
  username: string;
  surname: string;
  email: string;
  birthdate: string;
  phoneNumber: string;
};

export type CartItem = {
  title: string;
  description: string;
  option: string;
  quantity: number;
  price: string;
};
